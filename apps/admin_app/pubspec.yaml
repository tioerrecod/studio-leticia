name: admin_app
description: Studio Letícia — Beauty Command Center
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  design_system:
    path: ../../packages/design_system
  core:
    path: ../../packages/core
  go_router: ^13.0.0
  flutter_riverpod: ^2.4.9
  supabase_flutter: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
